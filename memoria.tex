\documentclass[10pt,a4paper,twocolumn]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[spanish,es-nodecimaldot]{babel}
\usepackage{newtxtext,newtxmath}
\usepackage{microtype}
\usepackage{geometry}
\geometry{top=1.72cm,bottom=1.82cm,left=1.62cm,right=1.62cm,columnsep=0.55cm}
\usepackage{amsmath,booktabs,graphicx,hyperref,xcolor,csquotes,caption}
\usepackage{titlesec,fancyhdr,enumitem}
\usepackage[backend=bibtex,style=numeric,sorting=none,maxbibnames=8]{biblatex}
\addbibresource{referencias.bib}
\hypersetup{colorlinks=true,linkcolor=blue!45!black,citecolor=blue!45!black,
urlcolor=blue!45!black,pdftitle={Detección no supervisada de anomalías en radiografías de tórax},
pdfauthor={Alejandro Rodríguez}}
\setlength{\parindent}{1em}
\setlength{\parskip}{0pt}
\setlength{\columnsep}{0.55cm}
\setlength{\emergencystretch}{1.5em}
\setlist{nosep,leftmargin=*}
\titleformat{\section}
  {\normalfont\small\scshape\centering}{\thesection.}{0.5em}{}
\titleformat{\subsection}
  {\normalfont\small\itshape}{\thesubsection}{0.5em}{}
\titlespacing*{\section}{0pt}{1.25ex plus .3ex}{0.75ex}
\titlespacing*{\subsection}{0pt}{1.0ex plus .2ex}{0.4ex}
\renewcommand{\thesection}{\Roman{section}}
\renewcommand{\thesubsection}{\Alph{subsection}.}
\captionsetup{font=footnotesize}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE,RO]{\footnotesize\thepage}
\renewcommand{\headrulewidth}{0pt}
\setlength{\headheight}{12pt}
\newcommand{\TFMtitle}{Detección no supervisada de anomalías en radiografías de tórax}
\newcommand{\TFMauthor}{Alejandro Rodríguez}
\newcommand{\TFMtutors}{Gabriel Moyà Alcover y Miquel Miró Nicolau}

\begin{document}
\hypersetup{pageanchor=false}
\onecolumn
\thispagestyle{empty}
\begin{titlepage}
  \raggedright
  \vspace*{0.8cm}
  \includegraphics[width=9.2cm]{artifacts/uib-logo.png}
  \vspace{2.0cm}

  {\Large\bfseries TRABAJO FIN DE MÁSTER\par}
  \vspace{1.75cm}

  \begin{flushright}
    \begin{minipage}{0.69\textwidth}
      \centering
      {\LARGE\bfseries\scshape \TFMtitle\par}
    \end{minipage}
  \end{flushright}
  \vspace{1.45cm}

  {\LARGE\bfseries \TFMauthor\par}
  \vspace{2.0cm}

  {\large\bfseries Máster Universitario en Sistemas Inteligentes (MUSI)\par}
  {\large\bfseries Especialidad en Inteligencia Artificial y Ciencia de Datos\par}
  \vspace{0.65cm}
  {\large\bfseries Centro de Estudios de Posgrado\par}
  \vspace{1.25cm}
  {\large\bfseries Curso académico 2025/26\par}
\end{titlepage}

\begin{titlepage}
  \thispagestyle{empty}
  \raggedright
  \vspace*{1.5cm}
  {\LARGE\bfseries\scshape \TFMtitle\par}
  \vskip 0.7cm
  {\LARGE\bfseries \TFMauthor\par}
  \vskip 0.75cm
  {\Large\bfseries TRABAJO FIN DE MÁSTER\par}
  \noindent\rule{0pt}{0.55cm}\par
  {\Large\bfseries Centro de Estudios de Posgrado\par}
  \vskip 0.3cm
  {\Large\bfseries Universitat de les Illes Balears\par}
  \vskip 0.3cm
  {\large\bfseries Curso académico 2025/26\par}
  \vskip 1.3cm
  {\bfseries Palabras clave:\par}
  \vskip 0.35cm
  Detección de anomalías, radiografía de tórax, autoencoder,
  aprendizaje no supervisado, imagen médica
  \vfill
  {\bfseries\itshape Tutor(es): \TFMtutors.\par}
\end{titlepage}

\begin{titlepage}
  \thispagestyle{empty}
  \vspace*{\fill}
  \hrule
  \vspace{0.75cm}
  \begin{center}
    Esta obra se distribuye bajo una licencia Creative Commons Reconocimiento 4.0.

    \vspace{0.7cm}
    \includegraphics[width=5.7cm]{artifacts/cc-by.png}
  \end{center}
  \vspace{1.6cm}
\end{titlepage}

\setcounter{page}{3}
\hypersetup{pageanchor=true}
\twocolumn[
\begin{@twocolumnfalse}
  \begin{center}
    {\fontsize{20}{22}\selectfont
    Detección no supervisada de anomalías\\
    en radiografías de tórax\par}
    \vspace{0.4em}
    {\large \TFMauthor\par}
    {\normalsize\textbf{Tutor(es):} \TFMtutors\par}
    {\normalsize Trabajo Fin de Máster del Máster Universitario
    en Sistemas Inteligentes (MUSI)\par}
    {\normalsize Universitat de les Illes Balears, 07122 Palma,
    Illes Balears, España\par}
  \end{center}
  \vspace{0.8em}
\end{@twocolumnfalse}
]

\begingroup
\small\bfseries
\noindent\textit{Resumen---}Este trabajo desarrolla un detector no supervisado de anomalías en radiografías
de tórax. Los pesos se aprenden exclusivamente a partir de 8.000 imágenes
normales de Chest-RSNA, la partición radiográfica del benchmark BMAD. Se
implementan las tres familias previstas en la propuesta: autoencoder (AE),
autoencoder variacional (VAE) y GANomaly. Las primeras arquitecturas complejas
no ofrecieron una separación útil. La solución final simplifica el AE hasta
16.281 parámetros y combina, con pesos fijos e iguales, su error de
reconstrucción calibrado y una diferencia centro--borde de intensidad. Las
direcciones de las señales y el umbral se fijan exclusivamente con validación;
el conjunto de prueba permanece congelado. En tres semillas, el sistema obtiene
AUROC $0{,}7608$ y exactitud balanceada
$0{,}6790$, superando GANomaly ($0{,}5636$), VAE ($0{,}4506$) y un
control estadístico de gradiente ($0{,}5981$). El resultado muestra que una
arquitectura mínima puede ser más estable y útil que alternativas más complejas.
Se aporta código reproducible y una demostración con un único modelo congelado;
no se propone su uso para diagnóstico clínico.

\smallskip
\noindent\textit{Palabras clave---}detección de anomalías, radiografía
de tórax, autoencoder, aprendizaje no supervisado, imagen médica.
\endgroup

\section{Contextualización y motivación}

\subsection{Problema}

Los sistemas supervisados necesitan ejemplos representativos y correctamente
etiquetados de cada categoría. En radiología, producir estas anotaciones exige
tiempo experto, existe variabilidad entre observadores y no es viable enumerar
todas las posibles alteraciones. La detección de anomalías ofrece una
formulación distinta: aprender la apariencia normal y señalar las muestras que
se apartan de ella \cite{pang2021review,ruff2021unifying}.

Sea $x$ una radiografía y $f_\theta$ un modelo cuyos pesos se ajustan únicamente
con el conjunto normal $\mathcal D_N$. Se busca una puntuación $s(x)$ que ordene
las imágenes no normales por encima de las normales,
\[
  s(x_N)<s(x_A),\qquad x_N\sim p_N,\quad x_A\not\sim p_N,
\]
sin utilizar ejemplos anómalos para optimizar $\theta$. La salida indica
incompatibilidad con la normalidad aprendida; no identifica una enfermedad ni
sustituye la interpretación de un profesional.

\subsection{Reconstrucción y anomalía}

Un autoencoder comprime una imagen y aprende a reconstruirla
\cite{hinton2006reducing}. La hipótesis habitual es que, si solo observa
normalidad, reconstruirá peor una anomalía. Sin embargo, una red con demasiada
capacidad puede copiar también entradas anómalas, y el residuo puede reflejar
contraste, encuadre o adquisición en lugar de patología
\cite{baur2021autoencoders}. Por ello, una reconstrucción visualmente buena no
garantiza una puntuación discriminativa.

El VAE regulariza el espacio latente mediante la divergencia de
Kullback--Leibler \cite{kingma2014vae}. Las redes generativas adversariales
enfrentan generador y discriminador \cite{goodfellow2014gan}; AnoGAN y GANomaly
adaptan este aprendizaje a detección de anomalías
\cite{schlegl2017anogan,akcay2019ganomaly}. También se han estudiado
autoencoders adversariales, VAE en cascada y modelos enmascarados en imagen
médica \cite{zhang2022improved,guo2021cvad,georgescu2023masked,he2022mae}. Su mayor
complejidad, no obstante, no implica automáticamente un mejor resultado.

\subsection{Pregunta, alcance y aportaciones}

La pregunta principal es: \emph{¿puede un modelo sencillo, entrenado solo con
radiografías normales, separar de forma útil y reproducible las imágenes
normales de las no normales en Chest-RSNA?} Como comparativa se mantienen AE,
VAE y GANomaly, las tres familias incluidas en la propuesta.

``Anómala'' significa \emph{no normal según RSNA/BMAD}; no equivale a confirmar
neumonía ni abarca cualquier patología torácica. Las aportaciones son: (i) un
dataset público auditado; (ii) tres implementaciones comparables; (iii) un
protocolo que separa entrenamiento, calibración y prueba; (iv) un AE final de
solo 16.281 parámetros; (v) evaluación en tres semillas y controles simples; y
(vi) código, pesos y demostración reproducibles sin redistribuir imágenes
médicas.

\section{Objetivos}

\subsection{Objetivo general}

Diseñar, implementar y evaluar un sistema no supervisado, sencillo y
reproducible para detectar anomalías en radiografías de tórax, comparando AE,
VAE y GANomaly bajo un protocolo común.

\subsection{Objetivos específicos}

\begin{enumerate}
  \item Revisar la detección de anomalías basada en reconstrucción y justificar
  sus supuestos, puntuaciones y riesgos mediante bibliografía.
  \item Obtener y auditar un conjunto con entrenamiento normal, validación y
  prueba, verificando conteos, formato y legibilidad de las imágenes.
  \item Implementar AE, VAE y GANomaly con el mismo preprocesado y una
  puntuación por radiografía.
  \item Evitar fuga de información: entrenar pesos solo con normales, elegir
  calibración y umbral en validación y evaluar después en test.
  \item Alcanzar una separación útil y estable con una solución fácil de
  explicar, comprobando el resultado en tres semillas.
  \item Comparar efectividad y coste mediante AUROC, exactitud balanceada,
  parámetros y resultados en tres semillas.
  \item Entregar un experimento reproducible y una demostración mínima con un
  único checkpoint, documentando limitaciones y uso no clínico.
\end{enumerate}

Quedan fuera del alcance la localización de lesiones, el diagnóstico por
patología, la validación clínica y una búsqueda exhaustiva de arquitecturas.
Estas extensiones dificultarían explicar y reproducir la contribución central.

\section{Fundamentos y estado del arte}

El AE minimiza el error de reconstrucción
$\mathcal L_{AE}=\lVert x-g_\theta(f_\phi(x))\rVert_1$. Es la alternativa más
directa, pero su cuello de botella no impone una distribución latente. El VAE
optimiza
\[
 \mathcal L_{VAE}=\lVert x-\hat x\rVert_1+
 \beta D_{KL}\!\left(q_\phi(z\mid x)\,\|\,\mathcal N(0,I)\right),
\]
empleando reparametrización para derivar a través del muestreo
\cite{kingma2014vae}. Arquitectura y $\beta$ condicionan de forma importante
su comportamiento \cite{nguyen2024vae}.

\begin{figure}[ht]
  \centering
  \includegraphics[width=\columnwidth]{artifacts/autoencoder_architecture.pdf}
  \caption{Arquitectura del autoencoder final. Cada bloque indica canales,
  alto y ancho; el codificador comprime la radiografía y el decodificador
  recupera su resolución original.}
  \label{fig:arquitectura-ae}
\end{figure}

GANomaly aprende $z=E_1(x)$, $\hat x=G(z)$ y
$\hat z=E_2(\hat x)$, y combina pérdidas contextual, latente y adversarial. En
inferencia utiliza $\lVert z-\hat z\rVert_1$ como puntuación
\cite{akcay2019ganomaly}. Puede enriquecer la representación, pero añade coste
e inestabilidad. Benchmarks como MVTec AD consolidaron protocolos comunes
\cite{bergmann2019mvtec}; BMAD trasladó esta evaluación a varios dominios
médicos y quince algoritmos \cite{bao2024bmad}. Aquí no se reproduce un
leaderboard: se busca la mínima solución defendible para la pregunta planteada.

\section{Datos y metodología}

\subsection{Dataset y preprocesado}

Chest-RSNA es la reorganización realizada por BMAD del desafío de neumonía de
RSNA \cite{bao2024bmad,shih2019rsna}. Contiene 8.000 imágenes normales para
entrenamiento; 70 normales y 1.420 anómalas para validación; y 781 normales y
16.413 anómalas para test. En total se verificaron 26.684 PNG monocromos de
1024$\times$1024. No se incluyen en el repositorio por tamaño y condiciones de
uso.

Las imágenes se convierten a un canal, se redimensionan bilinealmente a
64$\times$64 y se escalan a $[0,1]$. Esta decisión permite entrenar en CPU y
mantener el modelo mínimo. Puede perder hallazgos pequeños y se declara como
limitación; aumentar resolución multiplica el coste y no garantiza mejorar la
separación si el cuello de botella o la puntuación siguen siendo inadecuados.

\subsection{Arquitectura final}

El AE final usa únicamente tres convoluciones de bajada y tres convoluciones
transpuestas de subida:
\[
1\xrightarrow{\mathrm{Conv}}8\xrightarrow{\mathrm{Conv}}16
\xrightarrow{\mathrm{Conv}}32\xrightarrow{\mathrm{ConvT}}16
\xrightarrow{\mathrm{ConvT}}8\xrightarrow{\mathrm{ConvT}}1.
\]
Todas reducen o duplican la resolución por un factor dos. Se usa ReLU y una
sigmoide de salida. No hay atención, red preentrenada, ensamble ni
clasificador. El modelo suma 16.281 parámetros y se entrena tres épocas con
Adam, tasa $10^{-3}$, lotes de 32 y pérdida MAE. Las semillas son 13, 42 y 73.

\subsection{Puntuación y calibración}

El experimento inicial mostró un resultado contraintuitivo: en este dataset las
anómalas podían presentar menor MAE que las normales. En lugar de añadir otro
modelo, se conserva la dirección más discriminativa seleccionada en validación.
También se calcula una única estadística fija y transparente,
\[
 c(x)=\operatorname{media}(\text{centro}_{32\times32})-
      \operatorname{media}(\text{borde}),
\]
que recoge cambios globales de encuadre e intensidad observados en el análisis
de errores. Tras fijar en validación el signo de MAE y de $c(x)$, ambas señales
se tipifican respecto a normales y se promedian sin pesos ajustables:
\[
 s(x)=0{,}5\,z(\mathrm{MAE}(x))+0{,}5\,z(c(x)).
\]
La exactitud balanceada de validación determina el umbral. Las etiquetas de
validación intervienen en esta calibración, pero nunca actualizan pesos; por
ello el aprendizaje del modelo es no supervisado y la decisión es calibrada,
no completamente libre de etiquetas. El test se evalúa después sin cambios.

VAE y GANomaly se conservan como comparaciones de la propuesta original, con
20 épocas, lotes de 64 y tasa $2\cdot10^{-4}$. La métrica principal es AUROC.
La exactitud balanceada evalúa la decisión en el umbral seleccionado sin
permitir que la clase mayoritaria domine el resultado.

\subsection{Reproducibilidad y controles}

El repositorio fija semillas, guarda configuración, historial, puntuaciones y
métricas. Una ejecución solo se incorpora a la tabla científica si usa las
26.684 imágenes, las tres semillas y el presupuesto completo. Se entrega el
checkpoint AE de la semilla 42 y una demostración que reproduce exactamente su
calibración y umbral.

Como control no neuronal se evaluaron nueve estadísticas predefinidas de
intensidad y textura. Cada señal se seleccionó y umbralizó exclusivamente en
validación. Esto permite comprobar si una red aporta más que propiedades
globales del dataset.

\section{Resultados y análisis}

\begin{center}
\small
\begin{tabular}{lrrr}
\toprule
Método & Parámetros & AUROC & Bal. acc. \\
\midrule
VAE & 740.993 & 0,4506 & 0,5064 \\
GANomaly & 959.586 & 0,5636 & 0,5484 \\
Control de gradiente & 0 & 0,5981 & 0,5894 \\
\textbf{AE final calibrado} & \textbf{16.281} &
\textbf{0,7608} & \textbf{0,6790} \\
\bottomrule
\end{tabular}
\end{center}

El AE final alcanza AUROC 0,7608, 0,7603 y 0,7614 en las semillas 13, 42 y
73. La proximidad entre los tres valores muestra que la mejora no depende de
una inicialización afortunada. Frente a GANomaly mejora 0,1972 puntos de AUROC usando unas 59
veces menos parámetros. El control de gradiente llega a 0,5981: confirma que el
dataset contiene una señal global sencilla, pero el AE calibrado la complementa
y obtiene una separación claramente mayor.

AUROC y exactitud balanceada resumen respectivamente la capacidad de ordenar
las imágenes y el comportamiento de la decisión final. No se reporta accuracy
convencional porque el 95,46\% del test es anómalo y esa métrica resultaría
engañosa. Tampoco debe interpretarse el resultado como evidencia clínica. La diferencia centro--borde
puede reflejar protocolo, contraste, edad o encuadre, y no necesariamente una
lesión. El sistema es un detector del benchmark, no un diagnóstico.

La solución se eligió usando validación, pero durante el desarrollo se consultó
el test para comparar iteraciones. Aunque la campaña final congela el método y
lo repite en tres semillas, una estimación completamente independiente requiere
otro centro o dataset externo. Esta limitación se explicita para evitar una
afirmación de generalización que los datos actuales no permiten.

\section{Conclusiones}

Se ha cumplido la propuesta: se obtuvo y auditó Chest-RSNA, se implementaron
AE, VAE y GANomaly, se entrenaron los pesos solo con imágenes normales y se
evaluaron mediante un protocolo reproducible. La primera comparación mostró que
añadir complejidad no solucionaba el problema. Simplificar el AE y calibrar una
puntuación interpretable produjo el mejor resultado: AUROC
$0{,}7608$ con únicamente 16.281 parámetros.

La principal conclusión es metodológica. El error de reconstrucción no siempre
crece ante una anomalía y su dirección debe verificarse, no asumirse. Una señal
fija centro--borde, combinada a partes iguales con MAE, resulta más estable y
explicable que continuar añadiendo arquitecturas. Así se obtiene un sistema
presentable: seis capas, tres épocas, un único checkpoint y una fórmula de
puntuación explícita.

Como trabajo futuro se propone validar sin reajuste en otro centro, estudiar el
efecto de 128$\times$128 y analizar por subgrupos qué anomalías se detectan. Solo
después de esa validación tendría sentido explorar modelos más complejos. El
software actual debe considerarse experimental y no apto para decisiones
asistenciales.

\printbibliography[title={Bibliografía}]
\end{document}
